The notebooks inside each subfolder can be used to generate the datasets.

For synthetic data (pareto, queue, vectorAR), the notebooks can be used to generate the dataset from scratch; for real data (stock, bike), the notebooks process the raw data and generates the data in the desire format.